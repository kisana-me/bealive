<% provide(:title, "画像をアップロード") %>

<h1>画像をアップロード</h1>
<p>アイコンを変更するにはアップロード後にアイコン設定で画像を選択してください。</p>

<%= form_with(model: @image, data: { turbo: false }) do |form| %>
  <%= render "shared/error_messages", instead: @image %>

  <div class="">
    <%= form.label :name, class: "custom-label" %>
    <%= form.text_field :name, class: "custom-textfield", placeholder: "画像名(省略可・デフォルトはファイル名)" %>
  </div>
  <br />
  <div class="">
    <%= form.label :image, class: "custom-label" %>
    <%= form.file_field :image %>
  </div>
  <br />
  <div class="">
    <%= form.submit "アップロード", class: "custom-button" %>
  </div>
<% end %>
