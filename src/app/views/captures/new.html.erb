<% provide(:title, "リクエストする") %>

<h1>リクエストする</h1>
<p>
  相手がアカウントを持っていない場合は公開範囲を「全世界」または「リンクを知っている人のみ」にする必要があります。
</p>
<%= form_with(model: @capture) do |form| %>
  <%= render "shared/error_messages", instead: @capture %>

  <div>
    <%= form.label :sender_comment, class: "custom-label" %>
    <%= form.text_field :sender_comment, class: "custom-textfield", placeholder: "コメントを入力..." %>
  </div>
  <br />
  <div>
    <%= form.label :visibility, class: "custom-label" %>
    <%= form.select :visibility, Capture.visibilities.keys.map { |k| [t("activerecord.enums.capture.visibility.#{k}"), k] }, {} %>
  </div>
  <br />
  <div>
    <%= form.submit "作成", class: "custom-button" %>
  </div>
<% end %>

<br>

<div>
  <%= link_to "キャプチャー一覧へ戻る", captures_path %>
</div>
