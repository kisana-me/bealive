<% if @current_account %>

  <h1>新着</h1>
  <div id="captures">
    <% @captures.each do |capture| %>
      <p>
        <strong>送信先:</strong>
        <%= capture.receiver %>
      </p>

      <p>
        <strong>Uuid:</strong>
        <%= capture.uuid %>
      </p>

      <p>
        <strong>Name:</strong>
        <%= capture.name %>
      </p>
      <p>
        <%= link_to "みる", capture_path(capture.uuid) %>
      </p>
    <% end %>
  </div>
  <%= link_to 'リクエストする', new_capture_path %>

<% else %>

  <h1>BeAlive.</h1>

  <p>いつもの日常を相互確認。</p>

<% end %>

<p>
  近々、フォローしたり、見れたり、リクエストを送れるようになります！
</p>